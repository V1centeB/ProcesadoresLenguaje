main() {
    a = 0;
    n = 10;  // Número de iteraciones

    // Bucle for en estilo canónico
    for (a = 0; a < n; a = a + 2) {
        printf("%d ", a);  // Imprime el valor de 'a' en cada iteración
    }
    printf("\n");

}


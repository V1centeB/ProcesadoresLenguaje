// Pruebas para trad3.y - hasta el punto 7

// Declaraciones globales (punto 1)
int ep ;

// Función main (punto 2)
main() {
    // Pruebas de impresión con puts (punto 3). TODO: Aclarar si se puede usar puts dentro de main (ahora mi)
    //puts("Hola mundo");

    // Pruebas de impresión con printf (punto 4)


    if (v % 2 == 0) {
        puts ("es par") ;
        ep = 1 ;
        while (ep == 1) {
            printf("%d", ep) ;
            ep = 0 ;
        }
    } else {
        puts ("es impar") ;
        ep = 0 ;
        printf("%d", ep) ;
    }

}